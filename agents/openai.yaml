interface:
  display_name: "个人论文审校"
  short_description: "结构工程论文的写作导航、论证追踪、数值引用与证据审核"
  default_prompt: "Use $paper-auditor in the appropriate draft or deep mode to build an evidence-linked paper spine, review this manuscript, and produce a Chinese report without modifying the source."

policy:
  allow_implicit_invocation: true
