interface:
  display_name: "个人论文审校"
  short_description: "面向结构工程论文的一致性、逻辑、图表、术语与文献证据核验"
  default_prompt: "Use $paper-auditor to audit this manuscript with my configured default mode and produce an evidence-linked Chinese report without changing the source."

policy:
  allow_implicit_invocation: true
